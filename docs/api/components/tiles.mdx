---
id: tiles
title: Tiles
---

import { Props, BoxProps } from '../../core/components';

`Tiles` renders a grid of components with equal spacing between them.

`Tiles` accepts all [`View`](https://reactnative.dev/docs/view) props.

### Examples

Simple usage.

```jsx live
function() {
  return (
    <StacksProvider>
      <Tiles space={2} columns={3}>
        <Placeholder />
        <Placeholder />
        <Placeholder />
        <Placeholder />
        <Placeholder />
      </Tiles>
    </StacksProvider>
  )
}
```

Other `Stacks` components used in `Tiles`.

```jsx live
function() {
  return (
    <StacksProvider>
      <Tiles space={2} columns={2}>
        <Columns space={2}>
          <Column>
            <Placeholder />
          </Column>
          <Column>
            <Placeholder />
          </Column>
        </Columns>
        <Placeholder />
        <Placeholder />
        <Stack space={2}>
          <Placeholder height={46} />
          <Placeholder height={46} />
        </Stack>
      </Tiles>
    </StacksProvider>
  )
}
```

## Props

<Props
  data={[
    {
      name: 'columns',
      type: 'responsiveProp<int>',
      required: true,
    },
    {
      name: 'space',
      type: 'responsiveProp<float>',
      required: false,
    },
  ]} />

## Box Props

<BoxProps />
