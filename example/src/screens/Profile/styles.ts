import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  highlight: {
    fontWeight: '500',
    fontSize: 10,
    color: '#8a8a8a',
    textTransform: 'uppercase',
  },
  photoHeight: {
    height: 50,
  },
})
