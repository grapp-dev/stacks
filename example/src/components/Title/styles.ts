import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  root: {
    fontSize: 22,
    letterSpacing: 0.5,
    lineHeight: 27,
    textAlign: 'center',
    fontWeight: '600',
  },
})
