/* eslint-disable react-native/no-raw-text */

/* eslint-disable import/no-default-export */
import { Grid } from '@grapp/stacks';

const Page = () => {
  return <Grid columns={8} margin={4} gutter={4} />;
};

export default Page;
